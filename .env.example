# Environment Configuration
# Copy this file to .env and fill in your values

# Node Environment (development | production)
NODE_ENV=development

# Server Configuration
PORT=5000

# MongoDB Configuration
# Required: MongoDB connection string
MONGODB_URI=mongodb+srv://your-user:your-password@your-cluster.mongodb.net/?retryWrites=true&w=majority

# Session Configuration
# Required: Secret key for session encryption (change in production!)
SESSION_SECRET=your-secure-random-secret-key-change-in-production

# PostgreSQL Configuration (Required for Vercel deployment only)
# For Vercel: Use Neon, Supabase, or Vercel Postgres
# DATABASE_URL=postgresql://user:password@host:5432/database

# Optional: Frontend URL (for CORS, if needed)
# FRONTEND_URL=http://localhost:5000
